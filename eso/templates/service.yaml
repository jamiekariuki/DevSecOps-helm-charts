apiVersion: v1
kind: Service
metadata:
  name: {{ include "eso.fullname" . }}
  labels:
    {{- include "eso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eso.selectorLabels" . | nindent 4 }}
