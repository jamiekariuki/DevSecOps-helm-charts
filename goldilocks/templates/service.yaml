apiVersion: v1
kind: Service
metadata:
  name: {{ include "goldilocks.fullname" . }}
  labels:
    {{- include "goldilocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goldilocks.selectorLabels" . | nindent 4 }}
