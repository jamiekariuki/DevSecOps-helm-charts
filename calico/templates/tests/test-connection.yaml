apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calico.fullname" . }}-test-connection"
  labels:
    {{- include "calico.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calico.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
