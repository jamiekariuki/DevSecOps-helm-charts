apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kyverno.fullname" . }}-test-connection"
  labels:
    {{- include "kyverno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kyverno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
