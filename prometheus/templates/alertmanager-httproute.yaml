apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.alertmanager.HTTPRoute.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.alertmanager.HTTPRoute.syncWaves | quote }} 
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}          
      namespace: {{ .Values.gateway.namespace }} 
  hostnames:
    - {{ .Values.alertmanager.HTTPRoute.hostName }}           
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.alertmanager.HTTPRoute.path }}      
      backendRefs:
        - name: {{ .Values.alertmanager.HTTPRoute.serviceName }} 
          port: {{ .Values.alertmanager.HTTPRoute.port }}