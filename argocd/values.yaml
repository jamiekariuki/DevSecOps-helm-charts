gateway:
  name: eg
  namespace: gateway

namespace: argocd

argocd:
  HTTPRoute:
    name: argocd-route
    syncWaves: "1"
    hostName: "argocd.example.com"
    path: /
    serviceName: argocd-server
    port: 80

#httproute

app:
  HTTPRoute:
    name: app-route2
    syncWaves: "1"
    hostName: "dev.example.com" 
    path: /
    serviceName: frontend
    port: 80    